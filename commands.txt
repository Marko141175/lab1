2. mkdir lab1
   cd lab1
   git init

3. notepad l1.txt

5. git add l1.txt
   git commit -m "Initial commit"

7. git add l1.txt
   git commit -m "modify l1.txt"

9. git remote add origin https://github.com/Marko141175/lab1.git

10. git push -u origin master

11. git reset --soft HEAD^

12. git branch logging

13. git checkout logging
    notepad log.txt	
    git log

14. git push --set-upstream origin logging

15. git merge logging